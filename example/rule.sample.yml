rule:
  type: object
  disallow_other_keys: true
  keys:
    foo:
      optional: true
      rule:
        type: number
    bar:
      rule:
        type: object
        keys:
          baz:
            rule:
              type: string
